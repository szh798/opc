# Policy seed data

Place the curated OPC policy source file here:

`opc-policies-2026-05-07.sources.json`

The importer expects an array, or an object with a `policies` array. Each policy
must include `id`/`policy_id`, `region`, `title`, `status`, and `sources[]`.
